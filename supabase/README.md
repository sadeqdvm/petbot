# Supabase setup

1. Run `supabase db push`.
2. Create a storage bucket named `payments` for screenshot uploads.
3. Enable Email auth and create admin users.
4. Use Realtime subscriptions on `messages` and `conversations` tables.

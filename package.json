{
  "name": "petbot",
  "version": "1.0.0",
  "main": "api/webhook.js",
  "scripts": {
    "start": "node api/webhook.js",
    "test": "node --check api/webhook.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "axios": "^1.6.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.2"
  },
  "description": "WhatsApp pet consultation bot backed by Supabase PostgreSQL, Auth, Realtime, and Storage.",
  "engines": {
    "node": ">=20"
  }
}
